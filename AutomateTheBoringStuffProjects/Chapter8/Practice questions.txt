
Q:

1. What is a relative path relative to?
 To the programs currents working directory
Q:

2. What does an absolute path start with?
 /
Q:

3. What do the os.getcwd() and os.chdir() functions do?
Gets current working directory and the other changes the directory to user specified directory
Q:

4. What are the . and .. folders?
. -> This directory  ..-> parent folder
Q:

5. In C:\bacon\eggs\spam.txt, which part is the dir name, and which part is the base name?

Q:the dir name is C:\bacon\eggs . Base name is spam.txt

6. What are the three “mode” arguments that can be passed to the open() function?
Read, Write, append
Q:

7. What happens if an existing file is opened in write mode?
It will be overwritten from scratch . 
Q:

8. What is the difference between the read() and readlines() methods?
read() opens  as one large string. Readlines() opesna s a list of strings
Q:

9. What data structure does a shelf value resemble?
Dictionary